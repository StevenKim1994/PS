#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int solution(vector<vector<int>> inputData)
{
	int answer = 0;

	do
	{
		
	}while(input)
	

	return answer;
}

int main()
{
	int N, input;
	vector<vector<int>> inputData;
	cin >> N;

	for(int i = 0 ; i<N; i++)
	{
		vector<int> inputList;
		
		for(int j = 0; j<N; j++)
		{
			cin >> input;
			inputList.push_back(input);
		}
		inputData.push_back(inputList);
	}

	cout << solution(inputData) << '\n';

	return 0;
}